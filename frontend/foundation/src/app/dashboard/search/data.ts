export type Search = {
    id: number;
    title: string;
    description: string;
    created_at: number; // Assuming createdAt is a timestamp
}

export type SearchesMetadata = {
    id: number;
    title: string;
}